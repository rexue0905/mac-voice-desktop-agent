##  开发规则（模块化 + 小步提交）
- 一次只实现一个模块（模块 0 / 模块 1 / 模块 2 ...）。
- 每个模块完成后提交到 GitHub。
- 不提前实现未来模块，不做“顺便把后面也写了”的扩展。
- 优先稳定、清晰、可维护；不引入复杂框架。
- 默认不做架构优化，除非明确提出需求。

## 目录说明
- `src/`：后续代码目录（模块 0 为空是正常的）
- `docs/`：设计文档与说明（模块 0 仅放占位文件）